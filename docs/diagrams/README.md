# Диаграммы и иллюстрации

Файлы в этой папке используются в пояснительной записке и внутренней документации. **Источник правды для правок — SVG** (текстовый формат, правится в редакторе или Inkscape и т.п.).

| Файл | Содержание |
|------|------------|
| `figure-2-1-statistics-process.svg` / `.png` | BPMN: общий процесс работы со статистикой (рис. 2.1). Исходник BPMN: `figure-2-1-statistics-process.bpmn`. |
| `figure-2-2-match-statistics-process.svg` | BPMN: процесс получения статистики матча (рис. 2.2). |
| `figure-2-4-components.svg` / `.png` | Компоненты системы (рис. 2.4). |
| `figure-spa-architecture.svg` / `.png` | Архитектура SPA. |
| `figure-react-frontend-architecture.svg` / `.png` | Структура фронтенда. |
| `figure-flask-backend-architecture.svg` / `.png` | Структура бэкенда Flask. |
| `figure-ttl-cache.svg` / `.png` | Схема кэширования с TTL. |
| `figure-inflight-dedup.svg` / `.png` | Дедупликация параллельных запросов. |

**Рисунок 2.2:** растровый `figure-2-2-match-statistics-process.png` в репозитории не хранится — при необходимости экспортируйте из `figure-2-2-match-statistics-process.svg`, чтобы текст на схеме совпадал с актуальной версией.
